Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect users trying to access directories to index.php
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^(.*)$ /index.php [L]
</IfModule>
